plugins {
    id 'io.gatling.gradle' version "3.4.2"
}

repositories {
    jcenter()
}

gatling {
    // WARNING: options below only work when logback config file isn't provided
    logLevel = 'WARN' // logback root level
    logHttp = 'FAILURES' // NONE, ALL, FAILURES
}